<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">
        <%= image_tag("header/hobbyisten_logo.png", :class => "header-logo", :size => "38") %><!--Hobbyistenlago-->
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <!-- Suchfunktion nicht mehr implementiert
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="München, Berlin">
        </div>
        <button type="submit" class="btn btn-default">Suchen</button>
      </form> -->
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <a href="/">
              Vorschläge
            </a>
          </li>
          <li>
            <a id="myEventBadge" href="/events" class="eventBadge" data-badge="<%= current_user.events.size %>">
              Veranstaltungen
            </a>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle user-dropdown" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <%= image_tag(current_user.image.url, height: '20px', class: 'header_userpicture') %>
                <%= current_user.firstname %>
              <span class="caret">
              </span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'Profil', current_user %>
              </li>
              <li role="separator" class="divider"></li>
              <li>
                <%= link_to "Abmelden", destroy_user_session_path, :method => :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <%= form_for(resource, as: resource_name, url: session_path(resource_name), :html => { :class => "navbar-form navbar-right login-header"}) do |f| %>
            <div class="form-group">
              <%= f.email_field :email, autofocus: true, placeholder: 'Email-Adresse', :class => "form-control" %>
            </div>

            <div class="form-group">
              <%= f.password_field :password, autocomplete: "off", placeholder: 'Passwort', :class => "form-control"  %>
            </div>

            <%= f.submit "Anmelden", :class => "btn btn-default"  %>
          <% end %>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

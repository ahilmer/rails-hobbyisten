<%= form_for(@events_hobby) do |f| %>
  <% if @events_hobby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@events_hobby.errors.count, "error") %> prohibited this events_hobby from being saved:</h2>

      <ul>
      <% @events_hobby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
